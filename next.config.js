/** @type {import('next').NextConfig} */
const nextConfig = {
  // Tambahkan konfigurasi Next.js di sini jika diperlukan
  // Saat ini dibiarkan kosong untuk memastikan build berhasil
};

module.exports = nextConfig;
